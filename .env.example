# Gemini is optional. If missing/invalid/quota-limited, deterministic fallback messages are used.
# DATABASE_URL is optional. If missing, the system falls back to the in-memory catalog/default signals.
GEMINI_API_KEY=your_gemini_api_key_here
DATABASE_URL=postgresql://user:password@localhost:5432/momzworld
